meta(charset="UTF-8")
meta(name="viewport", content="width=device-width,initial-scale=1")
title Customer Sign Up - Mayondo Furniture & Wood
link(rel="stylesheet", href="/CSS/customersignup.css")
.registration-container
  h2 Customer Sign Up
  form#customerForm(action="/customer_register", method="post")
    .form-group
      label(for="fullname") Full Name
      input#fullname(type="text", name="fullname", required)
    .form-group
      label(for="email") Email
      input#email(type="email", name="email", required)
    .form-group
      label(for="phone") Phone Number
      input#phone(type="tel", name="phone", pattern="[0-9]{10,15}", placeholder="Digits only", required)
    .form-group
      label(for="address") Address
      input#address(type="text", name="address", required)
    .form-group.password-container
      label(for="password") Password
      input#password(type="password", name="password", required)
      button(type="button", onclick="togglePassword(\'password\')") 👁️
    .form-group.password-container
      label(for="confirm_password") Confirm Password
      input#confirm_password(type="password", name="confirm_password", required)
      button(type="button", onclick="togglePassword(\'confirm_password\')") 👁️
    button(type="submit") Sign Up
    //  <a href="login.html" class="btn btn-primary btn-lg">Get Started</a> 
script(src="/Javascript/customersignup.js")